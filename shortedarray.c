#include<stdio.h>
#include<conio.h>
int main()
{
int i,n;
clrscr();
inta[i];
printf("enter the numbers");
scanf("%d",&number);
for(i=0;i<n;i++)
{
printf("the array is a[i]");
}
printf{"the shorted array is %d:i,a[i]);
return 0;
}
