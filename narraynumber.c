#include<stdio.h>
int main()
{
int i,n;
int a[i];
printf("enter the n numbers");
scanf("%d",&n);
for(i=0;i<n;i++)
{
printf("enter the array n element");
scanf("%d",&a[i]);
}
printf("the array n element %d",i,a[i]);
return 0;
}
