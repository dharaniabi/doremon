#include<stdio.h>
int main()
{
char a[25];
int i,n,count=0,m,a;
printf("enter the character");
scanf("%d",&a);
m=strlen(a);
for(i=0;i<m;i++)
{
printf("a[i]!=' ');
count++;
}
printf("number of characters %d",count);
return 0;
}
